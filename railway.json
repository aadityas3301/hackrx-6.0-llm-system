{
  "name": "hackrx-6.0-llm-system",
  "build": {
    "commands": [
      "pip install -r requirements-minimal.txt"
    ]
  },
  "start": {
    "command": "uvicorn main_lightweight:app --host=0.0.0.0 --port=$PORT"
  },
  "environment": {
    "PYTHONPATH": "/app",
    "PORT": "$PORT"
  }
}
